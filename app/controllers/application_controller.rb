class ApplicationController < ActionController::Base

#  private

#  def authenticate_user!
#    unless current_user
#      redirect_to user_registration_path, alert: "You are not log in!"
#    end
#  end

#  def current_user
#    @current_user ||= User.find_by(id: session[:user_id]) if session[:user_id]
#end

end
